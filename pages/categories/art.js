import axios from 'axios';

const Art = () => {
  return <div>Art</div>;
};

export async function getServerSideProps(context) {
  //  const artData =
  return {
    props: {},
  };
}

export default Art;
